esphome:
  name: siren-control

esp8266:
  board: d1_mini

mqtt:
  broker: !secret mqtt_broker

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

api:

logger:
ota:
  platform: esphome

switch:
  - platform: gpio
    pin: D2
    name: "Yelp Siren"
